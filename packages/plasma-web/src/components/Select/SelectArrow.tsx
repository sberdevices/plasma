import styled from 'styled-components';
import { IconChevronDown } from '@sberdevices/plasma-icons';

export const SelectArrow = styled(IconChevronDown)`
    margin-left: 0.75rem;
    transition: color 0.3s ease-in-out, transform 0.15s ease-in-out;
`;
