import React from 'react';

import { CartButton } from './CartButton';

export default {
    title: 'Cart Button',
};

export const Default = () => (
    <div>
        <CartButton screen="cart" />
    </div>
);
