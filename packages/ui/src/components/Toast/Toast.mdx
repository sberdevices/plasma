---
name: Toast
menu: Components
route: /components/Toast
---

import { Playground, Props } from 'docz';

import { Toast } from './Toast';
import { ToastProvider } from './ToastProvider'
import { Button } from '../Button'

import { ThemeSelect } from '../../helpers/ThemeSelect'
import { ThemeBackground } from '../../helpers/ThemeBackground'

<ThemeSelect />

# Toast
Короткие текстовые подсказки. Могут вызываться только в текущем запущенном приложении как реакция на выполнение действия пользователем.

## Props
<Props of={Toast} />


## Примеры использования
Компонент ``Toast`` может использоваться для создания собственной системы подсказок.  

### Toast
<Playground>
  <ThemeBackground>
        <Toast text="Short Text Message Without Action" />
  </ThemeBackground>
</Playground>

## Система подсказок
Также предоставляется готовая система текстовых подсказок. Для использования необходимо обернуть приложение в ``ToastProvider``:

```jsx
import React from 'react';

import { ToastProvider } from '@sberdevices/ui';

function App() {
    return (
        <ToastProvider>
            <div className="App">
            Hello world
            </div>
        </ToastProvider>
    );
}

export default App;
```

После этого, с помощью хука ``useToast`` можно получить доступ к управлению подсказками.

```jsx
import React from 'react'
import { useToast, Button } from '@sberdevices/ui'

export const Component = () => {
    const { showToast, hideToast } = useToast()
    
    return (
        <Button onClick={() => showToast('text message')} />
    )
}
```

``showToast`` имеет опциональный аргумент position, отвечающий за позиционирование подсказки.
```jsx
type Position = 'top' | 'bottom';
```

``showToast`` имеет опциональный аргумент timeout, отвечающий за время отображения подсказки.  
Если аргумент не был передан, время отображения будет не ограничено. Скрыть подсказку можно через функцию ``hideToast``.  
