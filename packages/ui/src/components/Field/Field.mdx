---
name: Field
menu: Components
route: /components/Field
---

import { Playground, Props } from 'docz'

import { Field } from './Field'
import { Icon } from '../Icon/Icon'
import { ThemeSelect } from '../../helpers/ThemeSelect'
import { ThemeBackground } from '../../helpers/ThemeBackground'

<ThemeSelect />

# Field
Компонент ввода с кепшеном сверху.

## Props
<Props of={Field} />

## Примеры использования

### Базовое применение
<Playground>
  <ThemeBackground>
    <Field
      value="Title 🌚"
      title="Label"
      onChange={(e) => setValue(e.target.value)}
    />
  </ThemeBackground>
</Playground>

### С контентом слева и справа в виде иконок
<Playground>
  <ThemeBackground>
    <Field
      value="5"
      title="Custom range input"
      contentLeft={<Icon icon="minus" size="s" />}
      contentRight={<Icon icon="plus" size="s" />}
    />
  </ThemeBackground>
</Playground>
