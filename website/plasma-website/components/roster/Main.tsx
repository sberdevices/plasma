import styled from 'styled-components';
import { Container } from '@sberdevices/plasma-b2c';

export const Main = styled(Container).attrs(() => ({ as: 'main' }))`
    padding-top: 3.75rem;
    padding-bottom: 3.75rem;
`;
