---
id: icon
title: Icon
---

# Icon
SVG-пиктограммы. Могут быть монохромными или цветными.

## Использование
Установите пакет `@sberdevices/plasma-icons`:

```bash
npm install @sberdevices/plasma-icons
```
Используйте именной импорт.
Например, чтобы в проекте появилась иконка `download`, сделайте импорт `IconDownload`:

```tsx title="App.tsx"
import { IconDownload } from '@sberdevices/plasma-icons';

export default function App() {
    return (
        <IconDownload />
    );
}
```

## Размеры
Большинство иконок создавались с определенными размерами и используются только с ними.

### [Список иконок](https://plasma.sberdevices.ru/ui-storybook/?path=/story/content-icon--xs-size) XS16

```tsx live
import React from 'react';
import { IconPlus } from '@sberdevices/plasma-icons';

export function App() {
    return (
        <IconPlus size="xs" />
    );
}
```

### [Список иконок](https://plasma.sberdevices.ru/ui-storybook/?path=/story/content-icon--small-size) S24

```tsx live
import React from 'react';
import { IconHeartStroke } from '@sberdevices/plasma-icons';

export function App() {
    return (
        <IconHeartStroke size="s" />
    );
}
```

## Пользовательский цвет

```tsx live
import React from 'react';
import { IconHeartStroke } from '@sberdevices/plasma-icons';
import { critical } from '@sberdevices/plasma-tokens';

export function App() {
    return (
        <IconHeartStroke color={critical} />
    );
}
```
