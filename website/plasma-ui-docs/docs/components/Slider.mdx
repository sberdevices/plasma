---
id: slider
title: Slider
---

import { PropsTable, Description } from '@site/src/components';

# Slider
<Description name="Slider" />
<PropsTable name="Slider" />

```tsx live
import React from 'react';
import { Slider, DeviceThemeProvider, Container  } from '@sberdevices/plasma-ui';

export function App() {
    return (
        <DeviceThemeProvider>
            <Container>
                <Slider onChangeCommitted={() => {}} min={0} max={100} value={30} disabled={false} />
            </Container>
        </DeviceThemeProvider>
    );
}
```
